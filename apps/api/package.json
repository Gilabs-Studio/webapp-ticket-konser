{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env DROP_TABLES=true go run ./cmd/server/main.go",
    "dev:keep": "cross-env go run ./cmd/server/main.go",
    "build": "go build -o bin/server ./cmd/server/main.go",
    "start": "./bin/server",
    "test": "go test ./...",
    "lint": "golangci-lint run",
    "clean": "go clean && rm -rf bin"
  },
  "engines": {
    "node": ">=18"
  }
}

