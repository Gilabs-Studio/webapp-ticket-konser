{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node ./scripts/dev-go.js --drop-tables",
    "dev:keep": "node ./scripts/dev-go.js",
    "build": "go build -o bin/server ./cmd/server/main.go",
    "start": "./bin/server",
    "test": "go test ./...",
    "lint": "golangci-lint run",
    "clean": "go clean && rm -rf bin"
  },
  "engines": {
    "node": ">=18"
  }
}

